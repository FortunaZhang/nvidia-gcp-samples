# Raw SGLang baseline for Kimi K2.5 Native INT4 — multi-NIC g4-dynamo-mn cluster.
# Direct mirror of https://github.com/shivajid/sglang-rtx-pro-6000/blob/main/models/KimiK2.5/sglang-ss.yaml
# Differences from the g4-dynamo (single-NIC) version:
#   - Storage: emptyDir for hf-cache (auto-backed by 32x local NVMe SSD via the pool's
#     --ephemeral-storage-local-ssd=count=32 flag). Matches Google reference exactly.
#   - hostNetwork: true (unchanged) — host already has eth0 + eth1 from the multi-NIC pool,
#     so NCCL_SOCKET_IFNAME=eth0,eth1 actually engages both gVNICs. No pod annotations needed.
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kimi-k25-sglang
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kimi-k25-sglang
  serviceName: ""
  template:
    metadata:
      labels:
        app: kimi-k25-sglang
    spec:
      nodeSelector:
        cloud.google.com/gke-accelerator: nvidia-rtx-pro-6000
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
        # GPU pool on us-south1 is --spot (only approved quota path). Tolerate.
        - key: cloud.google.com/gke-spot
          operator: Exists
          effect: NoSchedule
        - key: cloud.google.com/gke-preemptible
          operator: Exists
          effect: NoSchedule
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: sglang
          image: docker.io/lmsysorg/sglang:v0.5.10.post1
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -c
          args:
            - |
              python3 -m sglang.launch_server \
                --model moonshotai/Kimi-K2.5 \
                --tensor-parallel-size 8 \
                --pipeline-parallel-size 2 \
                --nnodes 2 \
                --node-rank $POD_INDEX \
                --port 8000 \
                --dist-init-addr kimi-k25-sglang-master:5000 \
                --trust-remote-code \
                --kv-cache-dtype fp8_e5m2 \
                --reasoning-parser kimi_k2 \
                --tool-call-parser kimi_k2 \
                --mem-fraction-static 0.85 \
                --chunked-prefill-size 16384 \
                --host 0.0.0.0
          env:
            - name: POD_INDEX
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['apps.kubernetes.io/pod-index']
            # HF_TOKEN required: hf-cache is emptyDir, so each pod re-downloads the
            # model on cold start. Kimi K2.5 is a gated repo.
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hf-token-secret
                  key: HF_TOKEN
            - name: NCCL_DEBUG
              value: INFO
            - name: NCCL_SOCKET_IFNAME
              value: "eth0,eth1"
            - name: NCCL_IB_DISABLE
              value: "1"
            - name: NCCL_P2P_LEVEL
              value: SYS
            - name: NCCL_SOCKET_NTHREADS
              value: "8"
            - name: NCCL_NSOCKS_PERTHREAD
              value: "8"
            - name: NCCL_MIN_NCHANNELS
              value: "8"
          resources:
            limits:
              nvidia.com/gpu: "8"
          volumeMounts:
            - mountPath: /dev/shm
              name: dshm
            - mountPath: /root/.cache/huggingface
              name: hf-cache
          securityContext:
            privileged: true
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 10Gi
        # hf-cache on the pod's ephemeral storage — automatically backed by local NVMe
        # because the GPU pool was created with --ephemeral-storage-local-ssd=count=32.
        # Matches Google reference. Trade-off: each pod re-downloads on cold start
        # (~700 GB; 10–15 min on the multi-NIC 200 Gbps gVNIC).
        - name: hf-cache
          emptyDir: {}
---
# Pod-0 only: distributed init address (--dist-init-addr).
apiVersion: v1
kind: Service
metadata:
  name: kimi-k25-sglang-master
spec:
  type: ClusterIP
  selector:
    app: kimi-k25-sglang
    apps.kubernetes.io/pod-index: "0"
  ports:
    - name: dist
      port: 5000
      targetPort: 5000
---
# Pod-0 only: the inference endpoint. Pod-1 is the second leg of the distributed worker.
apiVersion: v1
kind: Service
metadata:
  name: kimi-k25-sglang-serving
spec:
  type: ClusterIP
  selector:
    app: kimi-k25-sglang
    apps.kubernetes.io/pod-index: "0"
  ports:
    - name: http
      port: 8000
      targetPort: 8000
    - name: metrics
      port: 8080
      targetPort: 8080
